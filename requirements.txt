# AI-SecOps-Framework - Python Dependencies
# Core Security Scanning
semgrep>=1.60.0
# trivy is a binary tool, not a pip package - install via: https://github.com/aquasecurity/trivy

# LLM Security Testing
# giskard requires Python <3.12 - install via: pip install giskard (Python 3.10-3.11 only)
langchain>=0.1.0
openai>=1.0.0

# Guardrails & Safety
# nemoguardrails requires MSVC++ Build Tools on Windows (dependency: annoy)
# Install via: pip install nemoguardrails (requires C++ compiler)

# Utilities
pyyaml>=6.0
requests>=2.31.0
rich>=13.0.0  # Terminal formatting for reports

# Observability (optional)
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp>=1.20.0

# SBOM Generation (optional)
cyclonedx-bom>=4.0.0
spdx-tools>=0.8.0

# Fuzzing (optional)
# atheris requires libFuzzer - Linux only. Install via: pip install atheris

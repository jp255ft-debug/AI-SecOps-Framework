#!/usr/bin/env python3
"""
AI-SecOps-Framework Setup
Compatibility shim for older pip versions.
The authoritative build configuration is in pyproject.toml (PEP 621).
"""
from setuptools import setup

if __name__ == "__main__":
    setup()
